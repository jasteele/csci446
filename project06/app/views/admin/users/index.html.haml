%h2 Users
- will_paginate @users
%table
  %tr
    %th Name
    %th Username
    %th Last Login
    %th Number of Games
  - @users.each do |user|
    %tr
      %td= link_to user.first_last, edit_admin_user_path(user)
      %td= h user.username
      - if current_user.last_login_at.nil?
        %td No Logout Time
      - else
        %td= h time_ago_in_words(user.last_login_at)
      %td= h user.games.count