<h1>Listing roles</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @roles.each do |role,count| %>
  <tr>
   
    <td><%= link_to role, edit_admin_role_path(role) %></td>
	<td><%= count %></td>
    
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New role', new_admin_role_path %>