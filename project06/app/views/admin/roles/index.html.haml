%h1 Listing roles
%table
  %tr
    %th Name
    %th Number of Users
  - @roles.each do |role|
    %tr
      %td= link_to role.name, edit_admin_role_path(role)
      %td= num_users(role)
%br/
= link_to 'New role', new_admin_role_path